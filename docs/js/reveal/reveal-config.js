/**
 * WizNerdZ reveal system — configuration.
 *
 * Everything tunable lives here: tier→video mapping, timings, rarity thresholds,
 * audio cue names. No magic numbers buried in animation code.
 *
 * Videos are swappable: drop a new file in docs/reveal/ and change the path.
 */
window.WIZNERDZ_REVEAL_CONFIG = Object.freeze({
  videoBase: "reveal",

  /**
   * Tier → reveal video. Keys match tier_id in mint/sealed_boxes.json.
   * `handoff` is how many seconds before the end the live spell starts taking
   * over, so the prerecorded ending cross-dissolves into the browser-rendered
   * portal. See REVEAL_VIDEO_SPEC.md for what those final frames must contain.
   */
  tiers: {
    premium_named: { video: "Tier1.mp4", label: "Named Premium", handoff: 1.6 },
    elite:         { video: "Tier2.mp4", label: "Elite",         handoff: 1.4 },
    rare:          { video: "Tier3.mp4", label: "Rare",          handoff: 1.4 },
    standard_bundle:{ video: "Tier4.mp4", label: "Standard Bundle", handoff: 1.4 },
    blind_single:  { video: "Tier5.mp4", label: "Blind Single",  handoff: 1.2 },
  },
  fallbackTier: "blind_single",

  /** Phase durations in ms. Scaled by `speed` for large bundles. */
  timing: {
    spellBuild: 2600,
    consumeUi: 1500,
    portalExpand: 900,
    blackHold: 420,
    crtLine: 700,
    videoFadeIn: 500,
    portalSettle: 600,
    popIn: 620,
    popGap: 340,
    legendaryFreeze: 900,
    legendaryLines: 1500,
    collapse: 1400,
  },

  /**
   * Bundles must not become tedious. Above `compactAbove` NFTs the per-item
   * timings compress toward `minScale` so a 9-NFT premium box still feels
   * punchy rather than a 9-beat drum solo.
   */
  pacing: { compactAbove: 3, minScale: 0.45 },

  /** Rarity tiers that escalate the reveal. Matched case-insensitively. */
  rarity: {
    high: ["legendary"],
    elevated: ["epic"],
    slight: ["rare"],
  },

  particles: {
    maxDesktop: 340,
    maxMobile: 130,
    /** Disintegration sample step in px — larger = fewer, cheaper particles. */
    sampleStepDesktop: 12,
    sampleStepMobile: 20,
  },

  audio: {
    enabled: true,
    cues: {
      spark: "spark",
      rune: "rune",
      crt: "crt",
      portal: "portal",
      buildup: "buildup",
      pop: "pop",
      rare: "rare",
      legendaryWarn: "legendary_warn",
      legendaryImpact: "legendary_impact",
      collapse: "collapse",
    },
  },

  terminal: {
    channel: "> ARCANE CHANNEL ESTABLISHED",
    anomaly: "ANOMALOUS MANA SIGNATURE DETECTED",
    unknown: "> UNKNOWN ENTITY DETECTED",
    shouldNotBeHere: "> THIS SHOULD NOT BE HERE",
    legendary: "LEGENDARY SUMMON",
  },

  /** Runes mixed with code — ancient magic generated by a computer. */
  glyphs: ["{", "}", "<", ">", "0", "1", "/", "#", "XCH", "λ", "Ϟ", "⟁", "⟠", "✧", "∴", "⌘"],
});
